# Frontend (React)

This folder contains a minimal Vite + React scaffold for the shopping UI.

Quick start (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

Build for production:

```powershell
npm run build
npm run preview
```

The dev server runs on `http://localhost:5173` by default.
